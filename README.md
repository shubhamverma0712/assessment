the query is using GET parameters on the URI /search

example to search a string /search?string=cali

example to search a pincode /search?pincode=59645

example to search a cityname /search?cityname=california
